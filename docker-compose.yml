version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:8080"
    environment:
      - DATABASE_URL=file:./data/projectshelf.db
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    depends_on:
      - db
